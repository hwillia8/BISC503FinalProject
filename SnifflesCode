#examples of sniffles code used in structural variant calling


    #--input: BAM file with reads aligned to reference, --vcf: Output file containing structural variants,
    #--reference: Reference genome used for alignment
sniffles     --input chlamy_reads.sorted.bam     --vcf chlamy_reads.sniffles.vcf     --reference $ref



    #Lists VCF outputs across multiple barcoded samples, checks file size (ls -lh), previews first 30 lines (head),
    #and counts number of variants (wc -l)
ls barcode*/b*sniffles.vcf
ls -lh chlamy_reads.sniffles.vcf
head -n 30 chlamy_reads.sniffles.vcf
wc -l chlamy_reads.sniffles.vcf





##Parallelization:
    #Lists multiple VCF files (from Clair3 and Sniffles) and uses GNU parallel with 8 jobs to process them simultaneously
ls b*.filtered.clair3.vcf.gz b*.filtered.sniffles.vcf.gz 2>/dev/null | parallel -j 8 ' ... '

